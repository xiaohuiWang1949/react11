const defaultState={
    listState:[],
    limit:"",
    page:""
}

export default (state=defaultState,action)=>{
    //console.log(action.type)
    switch(action.type) {
        
    }
    return state
}